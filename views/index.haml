.main.large-8.columns  
  %h2 Generate placeholder text with song lyrics

  %form
    .lineSelector
      %button.refresh{:type => 'button'}
        %span ⟳
      %span.lines 0
      %input{:type => 'range', :class=> 'amount', :min => @range[:min], :max => @range[:max], :value => @range[:value]}

    %p.result

.api.large-4.columns
  %h2 REST API

  %p
    Access our service remotely by calling
    %a{:href => '/api/n'}= "#{request.base_url}/api/n"
    where n is an integer from #{@range[:min]} to #{@range[:max]} representing the amount of lines you need. You will get these in JSON format.
    %br
    For the record, this is the exact same API this application itself queries when you use it.